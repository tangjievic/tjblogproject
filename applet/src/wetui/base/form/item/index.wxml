<view class="wet-form__item">
    <view class="label" wx:if="{{label}}">
        <text class="required" wx:if="{{required}}">*</text>
        <text>{{label}}</text>
    </view>
    <view class="item_box">
        <slot></slot>
    </view>
</view>