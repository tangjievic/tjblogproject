import { AUTH_DATA } from '../constants/index'
export default function adminmsg(state={},action:any){
    switch(action.type){
        // case AUTH_DATA:
        //     const newState = cloneDeep(state)
        default:
            return state
    }
}